import 'package:flutter/material.dart';

class MadrijFormPage extends StatelessWidget {
  const MadrijFormPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Madrij Form'),
      ),
    );
  }
}
